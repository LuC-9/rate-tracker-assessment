# Demo Artifacts

This folder captures a local walkthrough of the Rate Tracker stack requested in the assessment:

- Docker stack running locally
- Dashboard at `http://localhost:3000`
- API latest endpoint at `http://localhost:8000/rates/latest/`
- Row click chart interaction on the dashboard

See `steps.md` for the step-by-step log and `screenshots/` for captured evidence.
